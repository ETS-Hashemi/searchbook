% The per-drone state machine of the decision logic.
\begin{tikzpicture}[font=\small,
  st/.style={minimum width=2.7cm,minimum height=1.0cm,text width=2.5cm},
  lbl/.style={sbannot,align=center,fill=white,inner sep=1.5pt}]
\node[sbbox,st]      (nom) at (0,0)      {\textbf{Nominal}\\ follow $\pi_i$};
\node[sbboxalt,st]   (avo) at (4.6,2.4)  {\textbf{Avoiding}\\ \orca\ / DWA velocity};
\node[sbboxgreen,st] (rec) at (4.6,-2.4) {\textbf{Reconnecting}\\ fly to waypoint $k$ at $t_k$};
\node[sbboxgray,st]  (rep) at (9.6,0)    {\textbf{Replanning}\\ \dstarlite\ / \astar, re-check};
\draw[sbflow] (nom.north east) -- node[lbl,above left,pos=0.45]{predicted conflict\\ inside $\tau_h$} (avo.west);
\draw[sbflow] (avo.south) -- node[lbl,right,pos=0.5]{clear for\\ $n_{\mathrm{clear}}$ cycles:\\ search a waypoint} (rec.north);
\draw[sbflow] (avo.east) -- node[lbl,above right,pos=0.45]{drift $> d_{\max}$, or\\ $t - t_{\mathrm{enter}} > t_{\mathrm{avoid}}$,\\ or LP infeasible} (rep.north west);
\draw[sbflow] (rec.east) -- node[lbl,below right,pos=0.45]{no admissible waypoint:\\ too fast, blocked,\\ too costly, constraint} (rep.south west);
\draw[sbflow] (rec.west) -- node[lbl,below left,pos=0.5]{waypoint $k$ reached\\ at time $t_k$} (nom.south east);
\draw[sbflow] (rec.north east) to[bend left=12] node[lbl,pos=0.6,right]{conflict\\ again} (avo.south east);
\draw[sbflow] (rep.south) to[bend left=25] node[lbl,below,pos=0.5]{new path $\pi_i'$ from the current cell,\\ re-checked against the others} (rec.south east);
\draw[sbflow] (rep.north) to[out=120,in=60,looseness=6] node[lbl,above,pos=0.5]{no path: hover safely,\\ retry after $T_{\mathrm{replan}}$} (rep.north);
\draw[sbflow] (avo.north) to[out=120,in=60,looseness=6] node[lbl,above,pos=0.5]{every cycle:\\ new half-planes} (avo.north);
\draw[sbflow,dashed] (nom.south) to[bend right=30] node[lbl,below,pos=0.45]{plan changed by a re-check\\ (partial replanning)} (rec.south west);
\end{tikzpicture}
