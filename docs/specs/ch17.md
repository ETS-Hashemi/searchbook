# Chapter 17 — Optimal Sampling-Based Planning: RRT* and Informed RRT* (RRT* High, Informed Medium; Week 8)
File Overleaf/chapters/ch17-rrt-star.tex, label ch:ch17. Algorithm template. Target 13-15 pages.
Anchors: Karaman & Frazzoli, "Sampling-based algorithms for optimal motion planning", IJRR 30(7) 2011, 846-894; Gammell, Srinivasa & Barfoot, Informed RRT*, IROS 2014; Gammell et al., BIT*, ICRA 2015 (pointer).
Must cover: the RRT* modifications to RRT, each with a figure: Near (all vertices within radius r_n), ChooseParent (connect the new vertex to the near vertex that gives the lowest cost-to-come, subject to a collision-free segment), Rewire (for every near vertex, if going through the new vertex is cheaper and collision-free, change its parent); pseudocode; the CONNECTION RADIUS r_n = min(gamma_RRT* (log n / n)^(1/d), eta) with the condition gamma_RRT* > 2 (1 + 1/d)^(1/d) (mu(X_free)/zeta_d)^(1/d) - state it exactly, explain each symbol, and explain in plain words why the radius must shrink slowly; the k-nearest variant; a worked example: the same map as ch:ch16 at 200, 1000 and 3000 iterations with the current best path (generated three-panel figure) and a table of best cost vs iterations; the theorem of ASYMPTOTIC OPTIMALITY (statement: the cost of the best path converges to the optimum almost surely; proof idea in one page: the ball-covering argument along an optimal path; make clear what is and is not claimed) and the contrast with RRT (no convergence); computational cost per iteration (O(log n) expected near-neighbours; the extra collision checks); INFORMED RRT*: once a solution of cost c_best exists, any better path lies in the prolate hyperspheroid (ellipse in 2D, ellipsoid in 3D) with foci x_start and x_goal, transverse diameter c_best and conjugate diameter sqrt(c_best^2 - c_min^2); how to sample uniformly in it (sample the unit ball, scale by diag(c_best/2, sqrt(c_best^2-c_min^2)/2, ...), rotate by the rotation matrix taking e_1 to the start-goal direction, translate to the centre) with the formulas and pseudocode; a figure of the ellipse shrinking as c_best improves; a generated convergence figure: best cost vs iterations for RRT, RRT*, Informed RRT* averaged over seeds (pgfplots); BIT* and other batch/informed methods in one paragraph; a comparison table of all sampling planners in the book; implementation notes (gamma choice in practice, the cost of rewiring, incremental cost updates of subtrees after rewiring, kd-trees, anytime use: keep improving while flying); pitfalls (radius too small kills optimality, forgetting to propagate cost changes to descendants after a rewire, sampling the ellipse before a solution exists, rewiring through obstacles); drone box (anytime continuous 3D planning; using the CBS grid path as a first solution to seed informed sampling; when the swarm planner should call RRT* instead of A*); 8 exercises incl. the Week-8 coding exercise and a derivation of the ellipsoid sampling transformation.
Code: Overleaf/code/ch17_rrt_star.py: RRT* with Near/ChooseParent/Rewire and descendant cost propagation, Informed RRT* with ellipsoidal sampling, both in 2D/3D; self-test: paths collision-free, RRT* best cost is non-increasing over iterations, informed samples all lie inside the ellipsoid, and on the test map RRT* reaches a cost within 5% of the straight-line-with-detour optimum computed by a fine-grid A* inside the file. Script Overleaf/code/figures/gen_ch17_convergence.py.
Cite karaman2011sampling, gammell2014informed, gammell2015bit, lavalle2006planning, lavalle1998rrt.
