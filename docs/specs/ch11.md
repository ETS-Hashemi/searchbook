# Chapter 11 — Coupling and Constructive Methods: M*, Push-and-Swap, Push-and-Rotate (M* Medium, P&S Awareness)
File Overleaf/chapters/ch11-mstar-push-and-swap.tex, label ch:ch11. Algorithm template (lighter). Target 12-14 pages.
Anchors: Wagner & Choset, M*, IROS 2011 and "Subdimensional expansion for multirobot path planning", AIJ 219 (2015) 1-24; Luna & Bekris, Push and Swap, IJCAI 2011; de Wilde, ter Mors & Witteveen, Push and Rotate, JAIR 51 (2014) 443-492; Standley 2010 (operator decomposition / independence detection).
Must cover: the joint state space (product of single-agent graphs) and why naive joint A* is hopeless (branching (|actions|)^k), with a table of state-space sizes; INDEPENDENCE DETECTION and operator decomposition in one section (Standley): solve agents in groups, merge groups only when they conflict; M*: the key idea of subdimensional expansion - search the joint space but expand only along the individual optimal policies unless a collision occurs; collision sets C(v), backpropagation of collision sets to ancestors, the limited neighbour function that branches only over agents in the collision set; pseudocode; a worked example on a small grid showing the search graph staying one-dimensional until two agents meet (figure); properties: M* is complete and optimal given optimal individual policies and an admissible joint heuristic (sum of individual costs), with a proof sketch; recursive M* and ODrM* mentioned; Push-and-Swap: primitives push (move an agent toward its goal, pushing blockers aside) and swap (exchange two agents using a vertex of degree >= 3), the algorithm at a conceptual level with figures of push and swap on a small graph, and the completeness result on graphs with at least two unoccupied vertices; Push-and-Rotate: the rotate primitive on cycles and the completeness result for the remaining cases; explicitly: these are FEASIBILITY solvers, not optimal ones - solutions can be far from optimal (give a figure/number); a comparison TABLE of every MAPF method in the book (prioritized planning ch:ch08, CBS ch:ch09, ECBS ch:ch10, M*, Push-and-Swap/Rotate) with columns: complete?, optimal?, bounded?, scales with?, typical use, chapter; drone box (when a swarm planner should couple agents vs when to accept a feasible plan; why the book's architecture uses CBS/ECBS); 7-8 exercises incl. a coding exercise (implement independence detection on top of a joint A*, or implement push-and-swap primitives on a small graph) and a hand exercise on collision sets.
Code: Overleaf/code/ch11_mstar.py: joint A* baseline, independence detection, and a basic M* with collision-set backpropagation on grids; self-test: M* returns conflict-free plans whose cost matches joint A* on tiny instances. Optional figure generator for the state-space growth table/plot.
Cite wagner2011mstar, wagner2015subdimensional, luna2011push, dewilde2014push, standley2010finding, stern2019mapf, felner2017search.
