# Chapter 6 — Anytime Search: ARA* (priority Medium)
File Overleaf/chapters/ch06-arastar.tex, label ch:ch06. Algorithm template. Target 11–13 pages.
Accuracy anchor: Likhachev, Gordon & Thrun, NIPS 2003: fvalue(s)=g(s)+ε·h(s); OPEN, CLOSED, INCONS; ImprovePath runs while fvalue(s_goal) > min_OPEN fvalue; each state expanded at most once per ImprovePath; an improved state already in CLOSED goes to INCONS; after each iteration decrease ε, OPEN := OPEN ∪ INCONS with keys recomputed, CLOSED := ∅, publish the ε-suboptimal solution; bound cost ≤ ε·C*, and ≤ ε'·C* with ε' = min(ε, g(s_goal)/min_{s∈OPEN∪INCONS}(g(s)+h(s))).
Must cover: the anytime idea for drones; recap of weighted A* (ch:ch04) and why re-running it wastes work; role of INCONS and reusing OPEN; pseudocode; worked grid example with three iterations (ε=3,2,1): three-panel figure with path and cost per iteration (numbers from code) and trace table; suboptimality theorem with proof sketch; the practical bound ε'; properties (each state expanded at most once per iteration; complete for ε≥1); ε schedule; generated experiment figure: cost and cumulative expansions vs iteration, ARA* vs weighted A* from scratch; Anytime D* section (concepts only); implementation notes; pitfalls (inconsistency from inflation, forgetting INCONS, decreasing ε too fast); drone box (compute-limited replanning, first path within a deadline); 7 exercises incl. coding (implement ARA*, plot cost vs time) and a proof.
Code: Overleaf/code/ch06_arastar.py (exists, self-test passes): ARA* on grids yielding each improved path with its bound; self-test verifies bound vs optimal A* on random grids and final optimality. Script Overleaf/code/figures/gen_ch06_anytime.py. Cite likhachev2003ara, likhachev2005anytime, pohl1970heuristic, pearl1984heuristics, koenig2002dstarlite.
