# Chapter 5 — Incremental Search: LPA* and D* Lite (D* Lite Essential, LPA* Medium; Week 2)
File Overleaf/chapters/ch05-lpastar-dstarlite.tex, label ch:ch05. Algorithm template. Target 16–18 pages.
Accuracy anchors: Koenig & Likhachev, D* Lite, AAAI 2002 (final optimized version with key modifier k_m; keys [min(g,rhs)+h(s_start,s)+k_m ; min(g,rhs)]; ComputeShortestPath while TopKey < CalculateKey(s_start) or rhs(s_start) ≠ g(s_start); overconsistent case g>rhs → g:=rhs, update predecessors; underconsistent case g<rhs → g:=∞, update vertex and predecessors; Main loop: move robot, scan changed edges, k_m += h(s_last, s_start)); Koenig, Likhachev & Furcy, LPA*, AIJ 2004. LPA* searches start→goal; D* Lite searches backward from the goal so the moving start can change; heuristic consistent w.r.t. the start.
Must cover: why replanning from scratch is wasteful (figure); g and rhs, local consistency/over-/under-consistency (definitions), two-component key; LPA* pseudocode and a fully worked grid example with g/rhs annotations before/after an edge change and a trace table (numbers from code); D* Lite: reversed search, robot motion, k_m with a numeric illustration of why it is needed, full pseudocode, worked example: robot follows path, obstacle appears, path repaired (three-panel figure); properties (same paths as re-running A*; expansion bound; assumptions); generated experiment figure: expansions and time for D* Lite vs repeated A* as obstacles are added on a 50x50 grid; implementation notes (PQ with key updates/lazy deletion, infinity, scanning changed edges, termination); pitfalls (predecessor updates on underconsistency, heuristic direction, lexicographic keys, which edges change, when replanning is slower than A*); variants: Anytime D* (→ch:ch06), Field D*, Moving Target D* Lite, original D* of Stentz; drone box (replanning layer; predicted intruder region as cost change); 8 exercises incl. Week-2 coding exercise (implement/adapt D* Lite; change obstacles during execution; compare runtime with rerunning A*), hand trace, proof on k_m.
Code: Overleaf/code/ch05_dstar_lite.py: LPA* and D* Lite on 4-connected grids, A* baseline inside, worked examples as functions, self-test: D* Lite path cost equals A* after random obstacle insertions/removals over many seeds. Script Overleaf/code/figures/gen_ch05_replanning.py. Cite koenig2002dstarlite, koenig2004lpa, koenig2005fast, stentz1994optimal, likhachev2005anytime, ferguson2006field.
