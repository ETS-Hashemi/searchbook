# Chapter 7 — The Multi-Agent Path Finding Problem (Week 3)
File Overleaf/chapters/ch07-mapf-problem.tex, label ch:ch07. Problem chapter: exact labelled definitions (def:ch07-...), pseudocode (conflict detection), worked example, ≥4 figures, ≥3 tables, boxes, summary, 8 exercises, solutions and glossary files. Target 13–15 pages. Anchor: Stern et al., SoCS 2019.
Must cover: classical MAPF (k agents, undirected graph, unique starts/goals, discrete time, move/wait, synchronous, plan = set of paths); conflict types with figures: vertex, edge, swapping, following, cycle (defined per Stern et al.), which are forbidden in this book (vertex, swapping) and why; stay-at-target vs disappear-at-target (book uses stay-at-target: cost = last arrival time after which the agent never leaves); sum of costs (\sumcost) and makespan (\makespan) with formulas and an example where they disagree; feasibility vs optimality; NP-hardness for both objectives (Yu & LaValle 2013; Surynek 2010) with intuition; feasible solutions polynomial with ≥2 empty vertices (→ch:ch11); taxonomy table of solver families (optimal search-based CBS/M*/ICTS; bounded-suboptimal ECBS; unbounded prioritized/push-and-swap; reduction-based SAT/ILP; learned) with chapter refs; instance formats: .map ('.' free, '@'/'T' obstacle) and .scen, table of benchmark map families with caveat; conflict detection pseudocode: pairwise O(k²T) and hash-map O(kT), padding with stay-at-target; worked example: 3 agents on 4x4 grid, paths table, detection by hand, conflict-free plan, both objectives; space-time recap (→ch:ch04); "MAPF for drones" (3D lattices, kinematic constraints honig2016mapf, continuous time, why classical MAPF remains the right abstraction); drone box; pitfalls (stay-at-target conflicts after arrival, off-by-one, edge conflicts beyond swaps); 8 exercises incl. "create deliberate conflict cases" and coding a detector/validator.
Code: Overleaf/code/ch07_mapf.py (exists, self-test passes): MAPFInstance, validation, conflict detection (first/all), sum of costs, makespan, .map/.scen parser, worked example. Existing figures in Overleaf/figures/ch07/. Cite stern2019mapf, yu2013structure, surynek2010optimization, sharon2015cbs, silver2005cooperative, felner2017search, honig2016mapf, ma2017lifelong.
