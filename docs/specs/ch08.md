# Chapter 8 — Prioritized Planning and Space-Time A* (priority High; Week 3 coding)
File Overleaf/chapters/ch08-prioritized-planning.tex, label ch:ch08. Algorithm template. Target 13–15 pages. Anchors: Silver 2005 (CA*, HCA* with true-distance heuristic, WHCA*); Erdmann & Lozano-Pérez 1987; van den Berg & Overmars 2005; Čáp et al. 2015 (well-formed infrastructures); Ma et al. 2019 (PBS pointer).
Must cover: idea (plan agents in priority order; higher-priority paths are moving obstacles); reservation table of (v,t) and (edge,t) entries incl. stay-at-target reservations; Cooperative A* = space-time A* (ch:ch04) against the table, pseudocode; worked example: 3 agents where order matters — (1,2,3) succeeds, (3,2,1) forces detours or fails (figure + table of costs per order); HCA* with backward-Dijkstra heuristic (ch:ch03) and why Manhattan is weak in clutter; WHCA* (window w) for lifelong/online use; incompleteness (instance where every ordering fails though a solution exists — draw it) and suboptimality (best ordering worse than optimal); positive result: well-formed instances (each agent can reach its goal avoiding all other starts/goals) ⇒ complete for any order (proof sketch); ordering heuristics (longest path first, most constrained, random restarts) with generated experiment: success rate and sum of costs vs #agents, random orders vs longest-path-first, random 20x20 grids; PBS section (one page, concepts); implementation notes (set of tuples, goal-stay check, edge reservations, horizon); pitfalls; drone box (fast global planner for large swarms, failure modes, why the architecture uses CBS/ECBS); 8 exercises incl. Week-3 coding exercise, reservation-table hand exercise, proof on well-formed instances.
Code: Overleaf/code/ch08_prioritized.py: ReservationTable, internal space_time_astar, prioritized_planning(instance, order), internal validator, well-formedness test, ordering heuristics, worked examples; self-test: conflict-free plans on random instances; ordering example behaves as described. Script Overleaf/code/figures/gen_ch08_orders.py. Cite silver2005cooperative, erdmann1987multiple, vandenberg2005prioritized, cap2015prioritized, ma2019searching, stern2019mapf.
