# Chapter 4 — A* Search (priority Essential; Week 1)
File Overleaf/chapters/ch04-astar.tex, label ch:ch04. Algorithm template. Target 16–18 pages. One of the most important chapters.
Must cover: best-first search with f=g+h; exact definitions of admissible and consistent heuristics; lemma consistency ⇒ admissibility (proof); theorem: A* with admissible h returns an optimal solution when nodes may be re-opened (proof in the style of Hart–Nilsson–Raphael / Russell–Norvig); theorem: with consistent h no re-expansion is needed and f along expansions is non-decreasing (proof); inadmissible-heuristic counterexample figure; pseudocode (graph search, closed set, lazy PQ); worked example on a 6x6 grid with obstacles: figure of open/closed cells and final path, trace table of the first 8–10 expansions with g,h,f; grid heuristics Manhattan, octile (max(dx,dy)+(sqrt2−1)min(dx,dy)), Euclidean, Chebyshev with a table of admissibility/consistency per connectivity; dominance and "more informed ⇒ fewer expansions" (statement + proof sketch); tie-breaking; weighted A* with w-suboptimality bound (proof); generated experiment figure: expansions of Dijkstra vs A*(Manhattan) vs A*(octile, 8-conn) vs weighted A* (w=1.5,2) on random grids; FULL SECTION on space-time A*: state (v,t), wait action, vertex constraints (a,v,t) and edge constraints (a,u,v,t) as in MAPF (used by ch:ch08, ch:ch09), goal handling (agent stays at goal; "goal free after time T" test), reservation tables, time horizon/termination, worked mini-example with one vertex constraint forcing a wait (figure with time layers), pseudocode; implementation notes (hashing, priority tuple (f,−g,counter), parents, memory); pitfalls; drone box (low-level planner inside CBS/ECBS, replanner fallback, reference for D* Lite); 8–10 exercises incl. Week-1 coding exercise (grid A*, add time as state variable, visualise open/closed sets), proofs (octile consistency, weighted A* bound), heuristics for 3D lattices.
Code: Overleaf/code/ch04_astar.py: astar for graphs and grids, heuristics, weighted A*, space_time_astar(grid,start,goal,constraints,max_time) with vertex/edge constraints and goal-stay; self-test vs an internal Dijkstra, constrained search waits when required, reproduces worked-example numbers. Script Overleaf/code/figures/gen_ch04_expansions.py → .dat. Cite hart1968formal, hart1972correction, dechter1985generalized, pearl1984heuristics, russell2020aima, pohl1970heuristic, silver2005cooperative, stern2019mapf.
