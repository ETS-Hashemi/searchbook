# Chapter 20 — Trajectory Prediction: From Constant Velocity to LSTMs and Transformers (LSTM Essential, Transformer Awareness-to-High; Week 10)
File Overleaf/chapters/ch20-trajectory-prediction.tex, label ch:ch20. Algorithm template adapted to a family of methods. Target 16-18 pages.
Anchors: Hochreiter & Schmidhuber 1997 (LSTM); Alahi et al., Social LSTM, CVPR 2016; Gupta et al., Social GAN, CVPR 2018; Vaswani et al. 2017; Giuliari et al., ICPR 2021; Salzmann et al., Trajectron++, ECCV 2020; Rudenko et al., survey, IJRR 2020; Pellegrini et al. 2009 (ETH dataset); Goodfellow et al. 2016; Kingma & Ba 2015.
Week 10: "Constant-velocity baselines, LSTM prediction, horizon and uncertainty. Compare constant velocity vs LSTM on trajectory sequences. Measure ADE/FDE or comparable prediction errors. Milestone: you can quantify whether learning actually improves prediction."
Must cover: the problem statement (observe T_obs positions of an agent, predict the next T_pred; single-agent vs interaction-aware; deterministic vs multimodal); the METRICS with exact formulas: ADE (average displacement error over the horizon), FDE (final displacement error), minADE_k/minFDE_k for k samples, and collision-based metrics; how to evaluate honestly (train/val/test split by scene, no leakage, several horizons, report per-horizon curves); the BASELINES that must be beaten: constant velocity (CV), constant acceleration, Kalman-filter extrapolation from ch:ch18 (and why CV is embarrassingly strong on short horizons - cite the survey), with a figure of their predictions; SEQUENCE MODELS: the LSTM cell with the gate equations written out (forget, input, output gates, cell state, hidden state; the sigmoid/tanh roles), why gating helps with long sequences, an encoder-decoder (sequence-to-sequence) predictor for trajectories: input normalisation (relative displacements in an agent-centred frame), the decoder rolling out one step at a time, teacher forcing during training, MSE loss vs Gaussian negative log-likelihood (output mean and covariance per step so the prediction comes with uncertainty), training with Adam, early stopping; a small numeric example of one LSTM cell step (numbers from the code); the Social-LSTM idea (pool hidden states of neighbours in a grid) explained with a figure; TRANSFORMERS: scaled dot-product attention with the formula, multi-head attention, positional encodings for time steps, an encoder over the observed trajectory and a decoder producing the future, and attention ACROSS AGENTS for interaction-aware prediction (a figure of the attention pattern); why Transformers help at longer horizons and with many agents, and what they cost (data, compute); MULTIMODALITY and uncertainty: mixture-density outputs, sampling (GAN/CVAE pointers), ensembles, and how to turn a predicted distribution into a time-indexed occupancy region (ellipses per step) for the planner; the experiment of the chapter, executed with the book's own NumPy code: a synthetic dataset of drone trajectories (mixtures of straight flight, gentle turns and evasive manoeuvres with noise), CV vs KF-extrapolation vs a small NumPy LSTM trained by the code (a minimal but real implementation with backprop through time, or a documented gradient-free/least-squares readout if BPTT is too long to run in the self-test), ADE/FDE per horizon in a table and a generated figure; state clearly that on the synthetic straight-flight subset CV wins and where the learned model wins; a PyTorch version of the same model given as a listing (not executed in the self-test) for the reader's real experiments; integrating predictions into the hybrid system: predicted trajectory + uncertainty -> inflated radius for ORCA, cost region for D* Lite, chance constraints for MPC (pointers to ch:ch13, ch:ch05, ch:ch21, ch:ch24); implementation notes (normalisation, sequence lengths, batch construction, seeds, evaluating on held-out manoeuvres); pitfalls (leakage between train and test, predicting in absolute coordinates, comparing against a poorly tuned CV baseline, reporting only ADE without the horizon, ignoring multimodality at intersections); drone box; 8-10 exercises incl. the Week-10 coding exercise (compare CV vs LSTM, measure ADE/FDE) and a derivation of the attention formula's complexity.
Code: Overleaf/code/ch20_prediction.py: synthetic trajectory generator, CV/CA/KF-extrapolation baselines, ADE/FDE, a NumPy LSTM cell and a small seq2seq trainer (keep the self-test under ~60 s by using a tiny model and few epochs; make the training deterministic with a fixed seed), and the evaluation harness producing the table; self-test: metrics are zero for a perfect predictor, CV is exact on straight flight, the LSTM's training loss decreases, gate outputs are in (0,1). Script Overleaf/code/figures/gen_ch20_results.py.
Cite hochreiter1997lstm, alahi2016social, gupta2018social, vaswani2017attention, giuliari2021transformer, salzmann2020trajectron, rudenko2020human, pellegrini2009you, goodfellow2016deep, kingma2015adam.
