# Chapter 3 — Dijkstra's Algorithm (priority High; Week 1)
File Overleaf/chapters/ch03-dijkstra.tex, label ch:ch03. Algorithm template. Target 12–14 pages.
Must cover: single-source shortest paths with non-negative costs; wavefront intuition (figure: expansion rings on a grid with an obstacle); pseudocode with priority queue and lazy deletion, early-exit single-target variant (uniform-cost search); worked example on a 6–7 node weighted graph (figure + trace table with queue contents after every step) and a second example on a 5x5 grid showing the closed set growing; correctness proof (invariant: popped node has optimal g; why non-negative weights; negative-edge counterexample); complexity O((|V|+|E|) log |V|) with binary heap, remarks on Fibonacci heaps and dense graphs; variants: multi-source, backward Dijkstra from the goal as exact (true-distance) heuristic for A*/CBS low level (important for ch:ch04, ch:ch08, ch:ch09), bidirectional search, BFS as unit-cost case; implementation notes (heapq, dict g, parents, path reconstruction, tie-breaking) with listing; pitfalls (decrease-key vs lazy insertion, float equality, negative edges, stale-entry check); drone box (baseline planner and heuristic factory); generated figure: expansions vs grid size on random grids (pgfplots from .dat); 8 exercises incl. Week-1 coding exercise (grid Dijkstra, visualise closed set) and one on backward-Dijkstra heuristics.
Code: Overleaf/code/ch03_dijkstra.py: dijkstra(graph, source, target=None) → distances, parents; grid adapter; path reconstruction; backward heuristic table; self-test vs brute force and vs the worked example (example graph lives in the code). Cite dijkstra1959note, cormen2009clrs, russell2020aima, hart1968formal.
