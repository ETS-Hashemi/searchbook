# Chapter 25 — Evaluating a Hybrid Planner: Experiments, Metrics and Benchmarks (Week 12)
File Overleaf/chapters/ch25-experiments.tex, label ch:ch25. Methodology chapter (adapt the template: metrics with definitions, experiment design, scenario generation, statistics, reporting, reproducibility, worked mini-study, exercises). Target 12-14 pages.
Source: docs/core-idea.txt "Experiments to run" and "Metrics": factors - number of controlled drones (2, 4, 8, more if feasible); unknown external drones (0, 1, 2, multiple crossing trajectories); prediction quality (perfect state, noisy state, constant-velocity prediction, learned prediction); avoidance strategy (local-only, global-replan-only, hybrid local + replanning); constraints (no formation requirement vs formation-preserving; no communication radius vs bounded range). Metrics - collision rate, minimum separation, path length, travel time, makespan, sum of costs, replanning count, computation time, formation error, communication violations.
Must cover: why evaluation is where the research contribution is made or lost; every METRIC defined precisely with a formula and units, its estimator over runs, and what it is sensitive to (a long table); the distinction between safety metrics (collision rate, min separation), efficiency metrics (path length, travel time, makespan, sum of costs), effort metrics (replanning count, computation time per decision and total), and constraint metrics (formation error from ch:ch23, communication violations = time steps with an edge longer than R_comm or with lambda_2 = 0); the EXPERIMENT MATRIX from the plan as a table and the notion of a full factorial vs a fractional design; BASELINES (local-only, replan-only, hybrid; plus "no avoidance" as a sanity check and MILP from ch:ch22 as an offline optimum on tiny instances); SCENARIO GENERATION: random maps and start/goal sets (from ch:ch07's instance formats), intruder trajectory families (straight crossing, turning, evasive), seeds, difficulty control (density, crossing angle), and the requirement that all strategies see the same scenarios (paired design); STATISTICS: number of runs, mean and standard deviation vs median and IQR, confidence intervals, paired comparisons, effect sizes, success rate with a runtime cap, and how to plot (box plots per factor level, cactus/survival plots for runtime, Pareto plots of safety vs efficiency) - each plot type shown with a generated figure from a toy simulation run by the code; REPRODUCIBILITY: configuration files, seeds, logging every decision, versioning the code, saving raw per-run results, a results table template; a WORKED MINI-STUDY executed by the code: 2 and 4 drones, 0/1 intruders, three strategies, 20 seeds each, the resulting table and two figures, with a cautious interpretation; REPORTING for a paper or thesis chapter: what to put in a results section, how to state limitations, how to describe the hardware/software; the completion checklist of the plan re-stated as evaluation questions; the benchmark landscape (MAPF benchmark suite, gym-pybullet-drones) and how to connect the book's Python code to a physics simulator (a short guide, no code required beyond an interface sketch); pitfalls (unpaired comparisons, tuning on the test scenarios, reporting means without spread, hiding failed runs, comparing computation times across machines, collision rate without min separation); drone box (how the experiment matrix maps onto the architecture's switches); 8 exercises: design an experiment matrix, compute metrics from a given log, critique a flawed results table.
Code: Overleaf/code/ch25_evaluation.py: metric functions (all ten), a scenario generator with seeds, a toy simulator that plugs in "strategies" (use simplified stand-ins that call ch24-style logic copied into the file, or a documented abstract simulator), a runner that produces a results table (CSV/.dat) and summary statistics with confidence intervals, and the paired comparison; self-test: metric functions on hand-built logs give known values, the runner is deterministic for a given seed, and the paired design assigns identical scenarios to all strategies. Script Overleaf/code/figures/gen_ch25_study.py.
Cite stern2019mapf, panerati2021learning, honig2018trajectory, sharon2015cbs, vandenberg2011orca, barer2014ecbs.
